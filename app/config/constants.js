// In production, this would be an environment variable
export const ADMIN_PATH_PREFIX = '/_admin';

// List of allowed admin email domains
export const ALLOWED_ADMIN_DOMAINS = ['yourcompany.com'];

// Secure key for admin access (in production, use environment variables)
export const ADMIN_ACCESS_KEY = 'your-secure-key-here';